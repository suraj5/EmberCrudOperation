{{#if showUpdatedMessage}}
<div  form-group><div></div></div>
{{/if}}

<form class="form-group">

      <div class=' form-group col-md-offset-6'>
      </div>
      <div class=' form-group col-md-offset-6'>
        <p class='test'> Edit Singer name of {{model.title}}</p>
      </div>
      <div class=" col-md-offset-6 form-group">
        <input type="text" class="form-control" id= "singer" name= "singerName"/>
      </div>
     <div class=" col-md-offset-6 form-group">
       <button type="submit" {{action "updateRecord" model}} class="btn btn-primary">Submit</button>
     </div>
</form>

<div class="flash-messages">
  {{#each flashMessages.queue as |flash|}}
    {{flash-message flash=flash}}
  {{/each}}
</div>

<div class="form-group"></div>

<div class="form-group">
  <div class="col-xs-6">
    <button class= "btn-default btn-success btn">{{#link-to "singer"}}<a style="color:white"> Go back  </a>{{/link-to}}</button>
  </div>
</div>